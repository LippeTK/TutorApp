/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = nextConfig

module.exports = {
    basePath: '',
    images: {
      domains: ['images.unsplash.com'],
    },
    swcMinify: true,
  };